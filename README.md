# RNN-Small-project
This project implements a time series forecasting model using a Recurrent Neural Network (RNN) with LSTM architecture to predict future stock prices of TATASTEEL. The model learns patterns from past stock data and uses them to predict future values based on previous trends.
